const filters=['ALL','DUB','DUBSTEP','DRUM & BASS','AMBIENT','TECHNO','NEUROFUNK','ROOTS','REGGAE','WORLD','BASS'];
export default function ReleaseFilters({active,onChange,counts}:{active:string;onChange:(x:string)=>void;counts:Record<string,number>}){return <div className="flex flex-wrap gap-2 mb-10">{filters.map(f=><button key={f} onClick={()=>onChange(f)} className={`font-mono text-[10px] tracking-[.2em] px-4 py-2 rounded-full border transition-all ${active===f?'border-cyan-electric text-cyan-electric bg-cyan-electric/5':'border-white/10 text-white/45 hover:text-white'}`}>{f}{f!=='ALL'&&counts[f]?` (${counts[f]})`:''}</button>)}</div>}
