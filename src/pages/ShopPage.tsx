import {Helmet} from 'react-helmet-async'; import SectionHeading from '@/components/ui/SectionHeading'; import ArtworkFrame from '@/components/ui/ArtworkFrame'; import {shopProducts} from '@/data/shop'; import {siteConfig} from '@/data/site'; import {useCursor} from '@/store/cursorStore';
export default function ShopPage(){const {set}=useCursor();return <><Helmet><title>Shop | DEPTH UNIT</title></Helmet><main className="pt-32 md:pt-40 pb-32 px-6 md:px-10"><div className="max-w-7xl mx-auto"><SectionHeading eyebrow="[ SHOP ]" title="Wear the weight." subtitle="Support the music you love. Wear it proudly. Limited runs, designed to be lived in."/><div className="grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-8">{shopProducts.map((p,i)=><a key={p.id} href={p.url} target="_blank" rel="noreferrer" onMouseEnter={()=>set('shop','VIEW')} onMouseLeave={()=>set('default')} className={`group block ${i===0?'md:row-span-2':''}`}><ArtworkFrame src={p.image} alt={p.title} className="w-full transition-transform duration-700 group-hover:scale-[1.03]"/><div className="mt-4 flex items-center justify-between"><div className="font-display text-xl text-white group-hover:text-gradient">{p.title}</div><div className="font-mono text-[10px] tracking-[.3em] text-white/40">SHOP ↗</div></div></a>)}</div><div className="mt-16 text-center"><a href={siteConfig.shopUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-3 font-mono text-xs tracking-[.3em] text-white border border-white/20 rounded-full px-8 py-4 hover:border-cyan-electric">ENTER FULL SHOP →</a></div></div></main></>}
