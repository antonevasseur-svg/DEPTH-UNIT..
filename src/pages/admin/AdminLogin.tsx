import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {Lock,Mail} from 'lucide-react'; import {motion} from 'motion/react';
export default function AdminLogin(){const navigate=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');const [mode,setMode]=useState<'login'|'setup'>(()=>{try{return localStorage.getItem('du:admin-creds')?'login':'setup'}catch{return 'setup'}});const submit=(e:React.FormEvent)=>{e.preventDefault();setError('');const hash=(s:string)=>{let h=0;for(let i=0;i<s.length;i++)h=(h<<5)-h+s.charCodeAt(i);return h.toString(36)};try{if(mode==='setup'){if(password.length<6)return setError('Password ≥ 6 chars');if(password!==confirm)return setError('Mismatch');localStorage.setItem('du:admin-creds',JSON.stringify({email,h:hash(password)}));localStorage.setItem('du:admin-session','true');navigate('/admin/dashboard')}else{const c=JSON.parse(localStorage.getItem('du:admin-creds')||'{}');if(c.email===email&&c.h===hash(password)){localStorage.setItem('du:admin-session','true');navigate('/admin/dashboard')}else setError('Invalid credentials')}}catch{setError('Error')}};return <div className="min-h-screen flex items-center justify-center px-6 relative"><div className="absolute inset-0 -z-10 bg-[radial-gradient(circle_at_50%_50%,rgba(122,60,255,.08),transparent_60%)]"/><motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} className="w-full max-w-md"><div className="text-center mb-8"><img src="/assets/logo/depth-unit-logo.svg" alt="DU" className="h-12 mx-auto mb-6"/><div className="font-mono text-[10px] tracking-[.4em] text-cyan-electric mb-2">[ {mode==='setup'?'INITIALIZE':'ADMIN ACCESS'} ]</div><h1 className="font-display text-3xl text-white">{mode==='setup'?'First-time setup':'Sign in'}</h1></div><form onSubmit={submit} className="glass rounded-2xl p-8 space-y-5"><label className="block font-mono text-[10px] tracking-[.3em] text-white/40">EMAIL<div className="relative mt-2"><Mail size={14} className="absolute left-3 top-1/2 -translate-y-1/2 text-white/30"/><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="w-full bg-white/5 border border-white/10 rounded-lg pl-10 pr-4 py-3 text-sm outline-none focus:border-cyan-electric"/></div></label><label className="block font-mono text-[10px] tracking-[.3em] text-white/40">PASSWORD<div className="relative mt-2"><Lock size={14} className="absolute left-3 top-1/2 -translate-y-1/2 text-white/30"/><input type="password" required value={password} onChange={e=>setPassword(e.target.value)} className="w-full bg-white/5 border border-white/10 rounded-lg pl-10 pr-4 py-3 text-sm outline-none focus:border-cyan-electric"/></div></label>{mode==='setup'&&<label className="block font-mono text-[10px] tracking-[.3em] text-white/40">CONFIRM<input type="password" required value={confirm} onChange={e=>setConfirm(e.target.value)} className="w-full mt-2 bg-white/5 border border-white/10 rounded-lg px-4 py-3 text-sm outline-none focus:border-cyan-electric"/></label>}{error&&<div className="font-mono text-[10px] tracking-[.2em] text-magenta-hot">{error}</div>}<button className="w-full font-mono text-xs tracking-[.3em] py-3 bg-white text-ink-950 rounded-lg hover:bg-cyan-electric">{mode==='setup'?'INITIALIZE':'SIGN IN'}</button>{mode==='login'&&<button type="button" onClick={()=>setMode('setup')} className="w-full font-mono text-[10px] tracking-[.3em] text-white/40 hover:text-white">RESET CREDENTIALS</button>}</form></motion.div></div>}
